# 变更日志

本技能遵循[语义化版本](https://semver.org/lang/zh-CN/)：

- **主版本**：目录结构或不兼容的流程改动（交付物形态重划、流程步数变化等）
- **次版本**：新增能力——新增规范、新增 `references/` 文件、新增红线
- **修订版**：文字订正、示例细节、错字与表述统一

版本号写在 `SKILL.md` 的 frontmatter `version:` 字段，**每个版本对应仓库中的一个 git 标签**（`v1.0.0` …），可按标签取出对应版本。

> **回填说明**：本文件建立于 2026-09-17。`1.0.0`–`1.7.0` 的条目**按仓库提交记录回填**（每条注明提交 SHA 前 8 位），措辞取自提交信息与当次产出的文件；精确细节以该提交的 diff 为准。

## [1.9.0] - 2026-09-17

### 新增

- **第 0 步产出重构为 LessonContext 课情卡**：七组字段（课文 / 课型 / 课时 / 学情 / 教师 / 教学语境 / 约束），**每个字段必须带 `confirmed`／`inferred`／`missing`／`needs_verification` 四态标记之一**；禁止静默填充，`inferred` 必附推定依据
- 新增 `references/lesson-context.md`：完整字段表（含 YAML 形态）、四态判定决策树、对用户呈现的表格模板、三个对外栏目（「请核」/「缺失项」/「待核对」）的交付话术、与下游的四个对接点、六种常见违规、第 0 步检查清单、来源分级
- 新增硬规则：**原文文字 / 课时数 / 教材版本年份三类禁止标 `inferred`**——这三类只能 `confirmed`，或 `needs_verification`／`missing`（推定错会渗透到母本与每一份文档）
- 母本（第 5 步）新增要求：**开头原样回显课情卡**，并将 `missing` 与 `needs_verification` 单列成「待核对事项」置于正文之前
- `references/flow-checklist.md` 新增「输入层」检查清单七条；第 0 步的交接物由"篇目/课型/课时/学情/交付取舍"改为课情卡
- README 新增「课情卡（LessonContext，第 0 步）」一节，七步流程图的第 0 步随之改写

### 变更

- **红线 7 由一句话升级为可执行判据**：判定标准改为 LessonContext 的逐字段状态标记——**填了不标状态，就等于编造**；`needs_verification` 字样在母本与交付物中不许撤下。红线条数不变（仍为 16 条），仅重写第 7 条表述
- §五「给用户的呈现方式」新增开场动作：**先给课情卡表**，并单列「请核 / 缺失项 / 待核对」请用户确认或更正

### 说明

- 本次改动**不新增红线条数、不改流程步数**，属"新增能力"（次版本）：把既有的宣示性红线 7 变成可核对的结构化输入
- 来源：2026-09-17 的技能审查与《V2 架构可行性评估》——该项被列为性价比最高的 P0 改进（现行红线 7 只靠一句"标待核对"约束，缺少判定规则，执行中极易滑过）
- 与 V2 架构的关系：采纳其 §4.1 `LessonContext` 与 S1「未知信息四态标记」的思路；**未**采纳其 YAML 作为对用户的呈现形态（对用户仍用表格），也**未**引入其状态机、质量门等更大改造——那些列为后续评估项，不在本版范围

## [1.8.0] - 2026-09-17

### 新增

- **版本控制体系**：`SKILL.md` frontmatter 新增 `version:` 字段；新增本文件；README 新增「版本与更新」一节（版本号含义、按标签拉取指定版本、版本核对方法）
- 为仓库已有提交补打版本标签 `v1.0.0`–`v1.7.0`，历史首次可按版本定位
- `SKILL.md` 参考一节新增版本与变更记录的索引

### 说明

- 本次提交**只加版本信息，不改动流程、红线与规范内容**——避免"加版本控制"本身被记成一次流程变更

## [1.7.0] - 2026-09-16

`d1eb7bbd` 教学目标：三条各落一个不同的素养维度；教案补齐缺的两节

### 变更

- 母本教学目标加格式硬约束：三条**各落一个不同的素养维度、不许重复**；维度名用 2025 修订版正式四条，禁用旧口径合并名「审美鉴赏与文化传承」
- 四份 Word 互查清单新增两条：素养维度不重复；教案必须含「教学目标」与「教学重点与难点」两节
- 示例《劝学》的教学设计与教案按新约束重出

## [1.6.0] - 2026-09-16

`bfd07f02` 示例更新：《劝学》替换《故都的秋》

### 变更

- 示例整体换代：新增 `examples/劝学/`（3 课时 · 重点校 · 17 页课件 · 含四份 Word），删除 `examples/故都的秋/`
- README 新增新旧示例差异对照；「上来就跑六步」更正为七步

## [1.5.0] - 2026-09-16

`f2e6d920` feat: 补齐学案与说课稿体例规范（含来源分级）

### 新增

- `references/xuean-shuoke-spec.md`：学案「三案」体例、八个基本内容、语文分体裁栏目差异、量化约束；说课稿九节、篇幅按说课评分权重倒推
- 每条规则附校本／教研依据与可信度分级（A／B／C 级），拿不出依据的标"经验取值"
- 记下待办：这两份规范本应由格式权威 `chinese-lesson-design` 承担

## [1.4.0] - 2026-09-16

`1962c513` feat: 四份 Word 交付（教学设计/教案/学案/说课稿）

### 新增

- 第 7 步拆为 7.1 四份 Word 与 7.2 课件 PPT；新增"母本冻结"准入条件与"同改同废"规则（母本一改，四份 Word 与课件全部重出）
- `references/deliverables-word.md`：四份分节规范、Word 版式与落盘规范、四份互查清单、常见坑
- 红线由 12 条扩至 16 条（第 13–16 条由四份 Word 交付引出）

## [1.3.0] - 2026-09-16

`e973ca1d` 同步课件标准：页数上限 20 页、作者介绍页与影像蒸馏配图

### 变更

- 课件页数上限由 15 页放宽至 **20 页**（含标题页、作者介绍页、小结页）
- 新增作者介绍页规则（只留能进入文本理解的 1–2 个信息点）与影像蒸馏配图规范

## [1.2.0] - 2026-09-13

`aa448305` docs: 新增《故都的秋》全流程示例（教学设计 + 课件）

### 新增

- 首个跑通的完整示例：教学设计 md + 15 页课件

## [1.1.0] - 2026-09-13

`8117eb21`／`fd56304b`／`c7961a62`

### 变更

- 第 7 步接入 `chinese-lesson-design` 的「配套 PPT 交付标准」
- 底层技能改名 `gaoyi-chinese-lesson-design` → `chinese-lesson-design`，全库引用同步
- 补齐 frontmatter（`agent_created`、`read_when`）

## [1.0.0] - 2026-09-13

`27010e77` feat: gaozhong-yuwen-design-orchestrator — WorkBuddy skill

### 新增

- 初版：七步流程、四个卡口、分流表、三处已知冲突与裁决、十二条红线（继承五个底层技能）、参考索引

## 未发布

### 待修（来自 2026-09-17 的一次技能审查）

- `references/deliverables-word.md:20` 写"回填母本第十二节"，与其余四处"第十一节"冲突——应为**第十一节**（第十二节是分层参数）
- 说课稿篇幅两套口径并存：评分表给"说教学过程 40%"，结论却写"占全稿一半左右"。需择一，并让示例对齐（示例实测 32.3%）
- 补规则：母本 md 的 ASCII 板书，**转任何 `.docx`（含教学设计母本直排版）时必须重画为层级表格**。示例 `劝学_教学设计.docx` 目前仍是 ASCII 字符画，与本技能红线 16 冲突
- 学案示例缺「评价反馈栏」、版头字段不全；栏目顺序与 `deliverables-word.md` §2.3 表不一致
- README「依赖」需补运行第 7 步必需的外部技能 `tencent-docx` 与 `tencent-pptx`，并补"通道不可用时的降级路径"
- 交付物缺件时的替代表述规则（用户只要教案时，教案作业栏"题目正文在学案"无落点）
