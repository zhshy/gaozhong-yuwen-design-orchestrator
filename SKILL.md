---
name: gaozhong-yuwen-design-orchestrator
description: 高中语文（统编版）备课总控。支持普通备课与学科课程类精品课两种模式。接到完整备课任务后，判断该调哪几个技能、按什么顺序调、每一步交出什么、交接时检查什么，并把成果分别生成四份 Word 交付物（教学设计、教案、学案、说课稿），另配课件 PPT 与检测量规；报精品课时另出四件套（教学设计、学习任务单、课件、作业练习）与微课运行稿。当用户要设计一节课、单元整体设计、备公开课，或需要把课标依据、文本解读、教学点、课堂设计、教学设计、教案、学案、说课稿、课件 PPT、检测串成完整流程，或要把教学设计成果导成 Word 文档，或要报／参评基础教育精品课（学科课程类）时使用。
agent_created: true
version: 2.1.0
modes: [normal_lesson, quality_course]
read_when:
  - 设计一节课、单元整体设计、公开课备课
  - 备课流程编排、该调哪个技能、多技能如何接力
  - 四份 Word 交付物（教学设计 / 教案 / 学案 / 说课稿）的分节与生成
  - 教学设计、教案、学案、说课稿、课件 PPT、检测量规的完整交付
  - 报／参评基础教育精品课、学科课程类、精品课五件套
  - 学习任务单、作业练习、微课运行稿、课程目录节点核验
---

# 高中语文教学设计总控

本技能**不装新知识**。它的唯一职责是：接到一个完整备课任务后，判断该调哪几个技能、按什么顺序调、每个技能交出什么、交接时检查什么。

底层技能各自的分工：

| 技能                              | 解决什么问题        | 输出的中间产物               |
| ------------------------------- | ------------- | --------------------- |
| `gaozhong-yuwen-kebiao`         | 依据在哪里、要求到什么水平 | 任务群定位 + 学业质量水平 + 素养落点 |
| `sunshaozhen-text-analysis`     | 文本怎么读深        | 文本关键点（一望而知与一无所知）   |
| `wang-rongsheng-reading-design` | 教什么、按什么顺序教    | 教学点 + 三个台阶            |
| `xiaopeidong-qianqian-teaching` | 课堂上怎么落地       | 教学出口 + 主问题 + 朗读设计     |
| `chinese-lesson-design`   | 交付格式与课件长什么样    | 情境任务链、板书、分层作业、**课件 PPT** |

> **输出通道**：四份 Word 交付物由 `tencent-docx` 生成（本技能只规定"每份装什么、不装什么"，版式由该技能决定）；课件 `.pptx` 由 `tencent-pptx` 生成。两者都**不在**上面五个技能内。

> **一处待办（如实交代，不假装已被覆盖）**：`chinese-lesson-design` 的输出格式目前只定义了教案九项，**没有**学案与说课稿的分节规范；五个底层技能里也没有任何一个定义过它们。本技能已把这两份的规范补齐，落在 `references/xuean-shuoke-spec.md`（含每条的校本或教研依据），但**它本该由格式权威承担**。长期应迁到 `chinese-lesson-design`——那是一处独立仓库的改动，需另行评估。迁移前，本节即这两份文件的格式依据。

---

## 一、先分流：这个请求要不要走全流程

不是所有请求都该走完整流程。**上来就跑七步是形式主义。**

| 用户的请求                                  | 处理方式                                                       |
| -------------------------------------- | ---------------------------------------------------------- |
| "帮我设计《某篇》的教学" / "我要上一节公开课" / "这个单元怎么教" | **走全流程**（下节）                                               |
| "帮我解读一下《某篇》"                           | 只调 `sunshaozhen-text-analysis`                             |
| "这篇课文该教什么" / "教学点定在哪"                  | `sunshaozhen` + `wang-rongsheng` 两步                        |
| "这个导入/提问怎么改"                           | 只调 `xiaopeidong-qianqian-teaching`                         |
| "这篇属于哪个任务群" / "该达到水平几"                 | 只调 `gaozhong-yuwen-kebiao`                                 |
| "帮我出个教案模板" / "只要格式"                    | 只调 `chinese-lesson-design`                           |
| "把教案做成 PPT" / "要上课的课件" / "要课件"          | 只调 `chinese-lesson-design`（按其「配套 PPT 交付标准」；**先有教案再两阶段做 PPT**） |
| "生成 Word" / "导成 word 文档" / "要教学设计文档"     | 走第 7.1 步；**母本未冻结先补做全流程**（见第 7 步的准入条件）   |
| "只要说课稿"                              | 先确认母本（教学设计）在不在——**没有冻结母本，说课稿写不出来**；有则从母本第九节派生，见 `references/deliverables-word.md` |
| "只要学案"                               | 同上，从母本的课时安排、任务链、检测与作业派生                     |
| "这节课怎么测" / "出检测题" / "做评分量规"            | 只调 `gaozhong-yuwen-kebiao`（评价任务转化）+ `wang-rongsheng`（检测维度） |
| "作文怎么批"                                | 调 `gaoyi-writing-coach`（不在本流程内）                            |
| **"做一节《某篇》的基础教育精品课" / "我要报（参评）精品课"** | **走精品课九步**（见下「第三种模式」）                                  |
| **"做精品课五件套" / "微课＋教学设计＋任务单＋课件＋作业"** | 同上，明确五件套                                        |
| **"帮我查一下这个篇目在智慧平台有没有节点"** | **只做第 0.5 步**，只出节点卡与核验结论                        |
| **"帮我写精品课的学习任务单" / "作业练习"** | 第 7.2 / 7.3 步；**必须先有 `内容冻结` 的母本**，否则先补流程 |
| **"帮我写微课视频脚本" / "微课运行稿"** | 第 7.5 步；同上。**只出运行稿，不管技术参数**                     |
| **"精品课材料一致性检查" / "提交前检查"** | 第 8 步；只读审计 ＋ 交付报告                               |

**判断口诀**：用户要的是**一节课**，走全流程；用户要的是**一节课里的某个零件**，直接给那个技能。

### 第三种模式：`精品课`（学科课程类精品课）

第 0 步的 **`交付模式`** 字段有两个取值（⚠️ **不要与 `课型.模式` 混用**——后者是讲读／自读／活动，是"课型"不是"交付模式"）：

| `交付模式` | 何时用 | 流程 |
|---|---|---|
| `普通备课`（默认） | 只说"设计一节课""备公开课""单元整体设计" | **原七步，一字不动** |
| `精品课` | 出现"**报／参评／提交／五件套／微课／目录节点**"任一词 | **九步**（原七步 ＋ 第 0.5 步节点锁 ＋ 第 8 步材料一致性） |

**`精品课` 不是另一套教学理论**，而是让**同一份教学设计**变成"可提交的作品包"。五个底层技能在本模式下**一律不改**。规格见 `references/jpk-high-school-chinese/`，**执行前先读该子目录的 `README.md`**（含加载顺序）。

**本模式只覆盖"学科课程类"**。特殊教育／实验教学／人工智能教育／阅读课／科技教育五类**不覆盖**，用户提出时明说不做。

---

## 二、全流程：七步，每步有交付物和检查点

```
第0步 确认输入  →  第1步 定依据  →  第2步 读文本
                                        ↓
第4步 落课堂  ←  第3步 定教学点排台阶
     ↓
第5步 母本成稿（教学设计）  →  第6步 检测与反馈（回填母本，母本冻结）
     ↓
第7步 四份 Word 交付（教学设计 / 教案 / 学案 / 说课稿）+ 配套课件 PPT
```

> **为什么是七步**：五份成果同源。`教学设计` 是母本，`教案`／`学案`／`说课稿`／`课件` 全部由它派生。而母本必须等检测与量规回填后才完整——所以"检测"必须在"派生交付"之前。若把 Word 生成挤进第 5 步，就会出现"母本还没齐、四份文档旧了一版"的返工。

### 精品课模式走哪几步（`精品课`）

在七步上**只插两步**，其余保持不变：

```
第0步 确认输入（＋ 提交 第八组字段）
  ↓
第0.5步 官方节点锁          ← 新增（节点不通过即 阻断）
  ↓
第1步 定依据（依据链前移）  →  第2步 读文本  →  第3步 定教学点排台阶  →  第4步 落课堂
  ↓
第5步 母本成稿（＝ 母本 逻辑体）
  ↓
第6步 评价 ＋ 一致性审计（目标→教学点→活动→证据→评价）
  ↓
★ 内容冻结          （第一次冻结：教学设计内容）
  ↓
第7步 四件套派生
        7.1 教学设计  7.2 学习任务单  7.3 作业练习  7.4 课件  7.5 微课运行稿
  ↓
第8步 材料一致性 ＋ 精品课 终检 门禁        ← 新增
  ↓
★ 报送冻结       （第二次冻结：可提交）
```

**哪里不动**：第 2、3 步**完全不动**；第 1、4、5、6 步只做增强；第 7 步**分流**（普通模式出四份 Word ＋ 课件，精品课出四件套 ＋ 微课运行稿）；第 8 步**全新**。

**为什么精品课要两次冻结**：普通模式是"检测通过 → 母本冻结 → 出 Word／PPT"，一次就够。精品课的五件材料**彼此之间还会产生新偏差**——所以先冻内容、再派生、再查一致性，最后才允许提交冻结。见 `references/jpk-high-school-chinese/material-consistency.md` §1。

### 第 0 步｜确认输入：建一张课情卡，逐字段标状态

本步的产出**不是**"几问几答"，而是一张 **课情卡**——把这次备课的全部输入参数结构化。核心纪律只有一条：

> **每个字段都要带状态标记，一个都不许裸着放。** 填了不标状态，等于编造。

四种状态：

| 状态 | 含义 | 处理方式 |
|---|---|---|
| `已确认` | 用户明确说了 | 直接用；写进母本与四份 Word 的页眉 |
| `推断` | 用户没说，但**可从已有信息推定**（如"统编版"→版本；"公开课"→有听课教师） | 可用，但**必须在呈现时写明推定依据**，并入"请用户核"一栏 |
| `未提供` | 用户没说，且**推不出来** | **问用户**；问不到就留空，并在交付物里显式列出"缺失项"，不许拿默认值悄悄顶替 |
| `待核验` | 我方**记得但拿不准**（原文语句、课时数、教材版本年份） | **一律标注，不得直接当事实用**；能查就查，查不到便在母本与交付物显著位置保留"待核对" |

字段分七组；另有一个**路由字段** `交付模式`，和一个**精品课专属组** `提交`（对用户呈现时用表格，不给用户看 YAML）：

| 组 | 字段 | 备注 |
|---|---|---|
| **路由** | `交付模式` | `普通备课`（默认）/ `精品课`。**从请求判断**：出现"报／参评／提交／五件套／微课／目录节点"任一词即为 `精品课`；判不准就问用户。**注意不是** `课型.模式`——那个是讲读／自读／活动，属"课型" |
| 课文 | `题名` / `作者` / **`原文`** / `文体` / `教材` / `年级` / `学期` / `单元` | `原文`＝**课文原文的核对状态**（印次 / 字形 / 篇目修订）；`教材` 未说明先问，默认统编版必修上/下册（标 `推断`） |
| 课型 | `课型.类别` / `模式` / `子类` | `类别`＝单篇精读 / 单元整体 / 群文阅读 / 复习课 / 作文讲评；`模式`＝**讲读 / 自读 / 活动**（与 `交付模式` 无关） |
| 课时 | `时长.课时数` / `单课时长` | **`待核验` 最高发的字段**——课时数拿不准绝不凑整 |
| 学情 | `学生.学校层次` / `班级人数` / `已有基础` / `已知` / `难点` | `学校层次` 必问（重点校 / 普通校 / 薄弱校） |
| 教师 | `教师.偏好风格` / `回避项` / `强项` / `弱项` / `典型课时长` | 可缺省；缺省时标 `未提供`，设计按"无偏好的通用方案"走，并在母本注明"**本次未做教师适配**"。**画像不得进入学案与课件**（学生看得到） |
| 教学语境 | `教学情境.用途` / `高考导向` / `公开课` | `公开课` 影响第 5 步的呈现方式；`用途` 与 `高考导向` 连同教师画像共同决定**教学方向**（基础型 / 思辨型 / 高考型） |
| 约束 | `约束.作业时长` / `多媒体` | 可缺省 |
| **提交**<br>（第八组） | `提交.年度` / `类别` / `学段` / `学科` / `平台` / `官方节点名` / `教材版本` | **仅 `交付模式 = 精品课` 时必填**；普通模式不填、不显示。详见下方说明 |

**关于 `交付模式`（路由字段，两种模式都需要）**：

- 它决定走**七步还是九步**，必须在第 0 步就定下来——**开到一半再改，等于两套流程各做半遍**。
- 判断依据是**用户的说法**（见 §一的判断口诀），不是篇目难度、不是"这节课比较重要"。
- 三种规格容易混：`交付模式`（交付模式）／`课型.类别`（单篇精读等）／`课型.模式`（讲读／自读／活动）。**三者互不相干。**

**关于第八组 `提交`（只在精品课模式下存在）**：

- 它不是"第七组之外顺手多加的一栏"，而是**精品课模式的输入前提**——节点名、教材版次、年度、平台都在这里定。
- 其中 `官方节点名` **必须是从平台原文复制来的**，不许凭记忆写；`教材版本` 须到**出版社／版次／册次**。
- 这一组填完不等于节点就锁定了——**第 0.5 步还要做核验**（见下）。
- 普通模式下这一组**整体不出现**，不要因为"反正用不上"就先填上：填了就进了母本，会污染普通备课的交付物。

**必问四项**（缺哪项问哪项，不许臆造）：① 篇目 / 单元；② 课型；③ 课时数；④ 学情层次。

**可选项**：公开课与否、是否需融入地方资源、**交付物取舍**（默认四份 Word + 课件全出；用户只要其中某几份时，记明缺哪几份，缺的部分不生成、也不要留空壳文件）。

**硬规则（就是红线 7 的落地形态）**：

- **禁止静默填充**——不许因为"看起来应该这样"就填上而不标状态。
- **三类禁止推定**——**原文文字 / 课时数 / 教材版本年份**这三类，只许 `已确认`，或 `待核验`／`未提供`，**不许标 `推断`**。理由：这三类一旦推定错，会渗透到母本与每一份文档的每一页；它们的正确来源是"用户说的"或"查得到的原文"，不是推理。
- **`推断` 必附依据**——写清"从哪一句推出来的"，让用户一眼能否掉。
- **`待核验` 必冒泡**——凡涉及**原文语句**与**课时数**的字段，拿不准一律标 `待核验`，并在母本、四份 Word、课件的显著位置保留"待核对"字样；**不许因为影响观感就把它删掉**。
- **来源不可考就不写**——课文出处、版本年份、作者生卒年查不到就标 `未提供`，宁可缺，不许写错。

**这张卡在后面四个地方被用到**：① 母本（第 5 步）开头回显本卡，未决项单列一段；② 四份 Word 的**页眉**取 `题名` / `课型` / `时长.课时数` / `学生.学校层次`；③ 第 3 步"备两头"的"学生疑难处"直接取 `学生.已知` 与 `学生.难点`；④ 第 7 步交付时做一致性自检——四份文档的篇目名与课时数据一律以本卡为准，逐字比对。

> 完整字段表、四状态的判定决策树、对用户呈现的表格模板、以及与"未决项"配套的交付话术，见 `references/lesson-context.md`。**填卡前先读它，不要凭印象定状态。**

### 第 0.5 步｜官方节点锁（**仅 `精品课`**）

> 普通模式**没有这一步**，直接进第 1 步。

**为什么必须在设计之前**：官方规定学科课程类**原则上同一目录节点只推荐 1 节**。节点是范围约束，不是提交时才填的一个字段——它决定你能教什么，进而决定教材、单元、任务群、依据。一旦选错节点（名称不对、年级不对、教材版本不对），**后面全部工作都建立在一个不会被接受的选题上**。这是返工成本最高的一处。

**这一步只做三件事**：

1. 把第八组 `提交` 字段填全，其中 `官方节点名` **从平台原文复制**；
2. 按五道核验逐道判定：**节点存在 → 学段＝高中 → 学科＝语文 → 教材版本对应 → 年级对应 → 标题全等**；
3. 留痕：`来源.网址` ＋ `核验时间` ＋ `证据`（截图或原文摘录），并把五项核验**逐项**写成布尔，**不许合成一个总的"通过"**。

**三条阻断情形（其余四道同为难通过项）**：

- **官方节点不存在** → 停
- **`官方节点名 !== 申报节点名`**（不是字符串全等）→ 停
- **教材版本无法确认** → 停，可先记 `待核验`，补齐证据后复核

**两条不许做的事**：

- **不许用"相似度 95%"作提交依据**。模糊匹配只能用于**检索**，不能用于**判定**。标题是提交口径本身——`《劝学》` 与 `《劝学（节选）》` 是两个节点，相似度高恰恰说明它**可能是另一个**。
- **不许降级**。本技能没有智慧平台的查询接口；节点核验只能由用户提供截图／原文，或联网检索后标注来源与时间。**两条路都拿不到证据时，不得降级为"推定通过"**——按"核心正确性无法保障"处理，停止流程并说明。**这是本模式唯一一处不做功能降级的地方**，因为其余各步都能在流程内部被修正，而节点是流程的**输入**。

**未通过本步，禁止进入母本冻结。**

> 字段表、五道核验的逐项判据、取证要求与检查清单，见 `references/jpk-high-school-chinese/catalog-node.md`。**做这一步前先读它。**

### 第 1 步｜定依据

**精品课模式（`精品课`）的依据链要前移**：普通模式从"课标 → 任务群 → 水平"起，精品课须从**官方节点**起，串成七跳并**每一跳可回溯**——

```
官方节点 → 教材（出版社/版次/册次）→ 单元 → 任务群 → 水平 → 素养 → 目标
```

**判据**：从目标往回走，每一跳都要说得出上一跳是什么；任一跳说不出即断链。**最要紧的一条：节点是甲、依据是乙 → 阻断**（节点定《劝学》，依据链却从别的单元的条目接上来）。写法与断链清单见 `references/jpk-high-school-chinese/alignment.md` §1、§2。

普通模式**不写这一条**，按下面的原口径走。

调 `gaozhong-yuwen-kebiao`，产出：

- 本篇所属**学习任务群**（18 个中的哪个）
- 该任务群的**学习目标**要点
- 本次教学**对标学业质量水平几**（水平 2 = 必修要求；水平 4 供高考指向参考）
- **核心素养四方面**中本课主要落哪两方面（不要四方面平均用力）

**检查点**：任务群定位与 `chinese-lesson-design` 里的"单元—任务群对照表"是否一致？不一致以课标原文为准。

**注意一处张力（必须向用户说明）**：课标以**任务群**组织课程，而 `wang-rongsheng-reading-design` 对"大单元/群文/项目化"持**审慎**立场。遇到单元整体设计需求时，不要假装没有分歧——据实说明两条思路，让用户选。这一步的产出是后面所有步骤的边界。

### 第 2 步｜读文本

调 `sunshaozhen-text-analysis`，产出：

- **一望而知与一无所知**的对照（表层读法 / 深层问题）
- **还原**表（原生态 / 文本中的形象 / 变异类型）
- **比较**：找一个对照文本或对照写法，指出差异
- **矛盾**：文本中的隐性矛盾 + 成因 + 美学生成
- **核心提问 1–3 个**（交给第 4 步用）

**检查点**：每个分析点是否**落到了具体语句**？说不出是哪几个字、哪个动作在起作用，就是没做完，退回重做。**不给概念标签**（"通过……表现了……"是结论不是分析）。

**学情参数**：普通校侧重第 1、2 层还原（感知还原、情感逻辑）；重点校可加价值还原、形式还原、流派还原。

### 第 3 步｜定教学点、排台阶

调 `wang-rongsheng-reading-design`，产出：

- **备两头**：文本关键点（来自第 2 步）× 学生疑难处
- **教学点** = 两者的**重合处**（黄绿重合）。第 2 步找出的关键点如果学生本就会，不能当教学点
- **学情与起点**：已有经验 / 与终点的落差 / 路径选择
- **三个台阶**（表格）：环节 | 教学点 | 主要"学的活动" | 所需语文知识 | 用时

**"用时"的口径**：不是拍一个数，要按**七列时间项**（六类必计项 ＋ 教师讲授）逐项拆开算——独立阅读 / 小组讨论 / 汇报×组数 / 教师回应 / 书写 / 切换组织 / **教师讲授·齐读·机动**。台阶表的"用时"是这些项之和。算法与计提规则见 `references/time-budget.md`——**漏掉"汇报 × 组数"和切换时间，是课堂超时最常见的两个原因；而第七项「教师讲授」不单列，前六类之和就小于课时时长，Σ 便无从核对，教师画像也落不成数字。**

**活动形态要过教师画像这一关**：台阶的主要活动不得与课情卡里 `教师.回避项` 的形态冲突。若该教学点非该形态不可，**换形态**（如小组讨论 → "同桌两两互说 + 全班抽样"），并在母本注明替换理由——不许"照排 + 备注一句可调整"。

**检查点（本流程最要紧的一关）**：

- [ ] 教学点有学理依据，不是凭教参照搬
- [ ] 每个台阶写的是**"学的活动"**，不是"教师讲……"
- [ ] 未做文本外的思想感情延展
- [ ] 台阶数不超过三个
- [ ] 每个台阶的"用时"已按七列时间项拆过，**汇报写了组数、切换未写 0**
- [ ] 台阶主要活动形态不与教师画像的 `回避项` 冲突

**常见断裂**：第 2 步的解读成果丰富，但教学点一个都没用上——说明解读和教学两张皮，退回第 3 步重定。

### 第 4 步｜落课堂

调 `xiaopeidong-qianqian-teaching`，产出：

- **教学出口**：从文本中长出的一句话、一个词、一个标点，或学生的疑问/误读
- **主问题**：与第 2 步的"核心提问"合流，收敛为**唯一一个主问题**；每个主问题**下方附一行 `> 方向备注：…`**——用一句话分别写出**基础型 / 思辨型 / 高考型**三种问法的差异。三句必须指向**不同层级**（读懂 / 评价 / 迁移），只换措辞不算；每句 ≤ 30 字。写法与示例见 `references/differentiation.md`
- **朗读设计**：每次朗读写清"为什么这里要慢/重/停"，回答不了就不设这个朗读
- **课堂风险点（原"课堂生成预案"）**：2–3 处，每处写成**三段式**——

  `① 学生可能怎么想错（错误形态，具体到会说出哪句话）→ ② 错在哪（误读机制）→ ③ 教师如何追问（追问语，可直接照读）`

  只写"学生可能答偏 + 追问方向"不算完成——那不是预案，是标签。

  > 例（《劝学》）：① 学生可能把"青，取之于蓝，而青于蓝"直接对号成"学生超过老师" → ② 把比喻的**关系**当成了**本体对应**，用日常成语顶替原文语境 → ③ 追问："荀子这里说的是颜色，还是说人？先把'蓝'和'青'各指什么说清楚，再说后半句。"

- **六维自查**（导入 / 提问 / 掌控 / 朗读指导 / 语言表达 / 文本解读）

**精品课模式另加一条**：**每个环节都要有学习证据**。即每个活动都要说得清"学生留下了什么"——一句话、圈画痕迹、表格、批注。留不下的活动，在精品课里无法进入评价链（`目标→教学点→活动→证据→评价` 的 `证据` 一节，见第 6 步）。

**检查点**：所有活动都能**指回具体的字词句**。指不回去，就是飘了。**风险点未写成三段式（缺"误读机制"或缺可直接照读的追问语）的，退回重做。**（精品课模式下另加：每个活动都有对应的学习证据。）

### 第 5 步｜母本成稿（教学设计）

调 `chinese-lesson-design` 的输出格式，把前四步的产物装进**教学设计母本**。母本是全部交付物的**唯一事实源**，分"十二节 + 两附"：

> **精品课模式（`精品课`）注意**：母本在逻辑上就是 **母本**——**不另造第二份母本**。
> 精品课的五件材料全部从**这同一份**母本派生。两个事实源必然数据不一致，这是最常见的失分来源。
> "精品课母本"只是流程里的**逻辑名**，物理主体仍是这里的十二节 ＋ 两附。加载顺序见 `references/jpk-high-school-chinese/README.md`。

**母本的头一块内容是第 0 步的课情卡**——原样回显那张卡（含每个字段的四态标记），并把所有 `未提供` 与 `待核验` 字段单列成一段「**待核对事项**」放在正文之前。这不是附注，是母本的组成部分：哪几个字是推定来的、哪几处还没核实，读者翻开第一页就该看见。落到附二（教材与课标版本核对说明）时，再把 `教材` / 版本年份的核对结论写清。

| 节 | 内容 | 来源 |
|:-:|---|---|
| 一 | 依据（任务群 / 学业质量水平 / 素养落点） | 第 1 步 |
| 二 | 教学定位（本篇在单元中的位置） | 第 1 步 |
| 三 | 教学目标（3 条，**各落一个不同的素养维度、不重复**，可观测行为动词） | 第 3 步教学点 |
| 四 | 教学重难点 | 第 3 步 |
| 五 | 课时安排表（**逐行拆到七列时间项**，Σ = 总时长，误差 0） | 第 3、4 步 |
| 六 | 情境任务链 | 第 3、4 步 |
| 七 | 主问题设计 + 追问链（**每问附一行"方向备注"**） | 第 4 步 |
| 八 | 板书设计 | 第 4 步 |
| 九 | 分层作业（基础 / 提高 / 拓展） | 第 4、6 步 |
| 十 | 教学风险点与应对（2–3 处，**三段式**） | 第 4 步 |
| 十一 | 检测任务与评分量规 | **第 6 步回填**（此处先留占位） |
| 十二 | **教学方向 × 学情适配**（3 方向 × 3 学情）+ 教师适配调整 | 全程 |
| 附一<br>附二 | 四卡口自检<br>教材与课标版本核对说明 | 全程 |

**合并规则**：母本的"教学目标"直接来自第 3 步的教学点，不要另起炉灶重写——另写就是两张皮。

**格式（三条目标，一条一行）**：`序号. **素养维度名**｜学生能……`。**三条各落一个不同的素养维度，不许重复**——三条里出现两个"思维发展与提升"、而"审美鉴赏与创造"或"文化传承与理解"缺位，就是没分列。维度名用 2025 修订版的正式四条（语言建构与运用 / 思维发展与提升 / 审美鉴赏与创造 / 文化传承与理解），**不用旧口径的合并名「审美鉴赏与文化传承」**。每条须能追回第 3 步的某一个教学点，条数＝教学点数。

> ⚠️ **例外只在精品课模式**：`交付模式 = 精品课` 时，本条放宽为"目标 **≤ 3 条**；每条须可观察、可评价、有学习证据、可回指教学点；**不再要求三条分属三个不同维度**"。
> **普通模式（`普通备课`）硬规则一字不动。** 判断依据是第 0 步的 `交付模式` 字段，**不是"感觉这次比较特殊"**。
> 放宽的边界与"仍然禁止"的表述见 `references/jpk-high-school-chinese/alignment.md` §5。

**检查点**：第 2–4 步的卡口是否都已过？第五节的时间明细是否**七列齐、汇报乘了组数、切换未写 0**，Σ 是否等于总时长（误差 0）、分项之和是否等于各环节小计？第十二节**主打方向那一行是否三格填满**？未过的退回对应步骤。**此步不出 Word、不出 PPT**——母本还没齐，先出就是返工。

### 第 6 步｜检测与反馈（闭环的关键一环）

前五步解决"怎么教"，这一步解决"**教完怎么知道学生到了**"。没有这一步，教案是开环的。

**精品课模式（`精品课`）在这一步多一道审计**：**教学评一致性**。主链必须闭环——

```
目标 → 教学点 → 活动 → 证据 → 评价
（目标 → 教学点 → 课堂活动 → 学生产出 → 学习证据 → 评价）
```

**缺任一节 → 阻断**（门禁四，本模式**最高优先级**门禁）。理由是：这条不过，其余判据全通过也无意义——**材料齐全但逻辑不闭环，正是精品课最普遍的问题**。

主链上的每一节都向四件交付物派生（任务单／作业／课件页／微课分镜）；**每个交付件里的每个元素都要能指回主链的某一节，指不回去的删掉**。断链检查、十列 `教学评对齐矩阵` 与七类断链编号见 `references/jpk-high-school-chinese/alignment.md` §4、§6。

调两个技能：

**（1）`gaozhong-yuwen-kebiao` → `references/ceping-renwu-zhuanhua.md`**

- 用三步法把第 1 步定下的学业质量水平**转成可用的评价任务**：定位（对应哪条水平描述）→ 拆解（拆成可观测行为）→ 定级（写分档表现描述）
- 任务形态按课标三类情境选：个人体验 / 社会生活 / 学科认知
- 产出**评分量规**（课堂用三档即可：未达标 / 达标 / 优秀，标注对应水平号）

**（2）`wang-rongsheng-reading-design` → `references/detection-design.md`**

- 先定维度再定题型：测"**看出**"（结果维度）还是"**看到**"（过程维度）
- 按体裁选题型：**实用文**（说了什么）→ 反应性测试，可用客观题；**文学文本**（怎么说的）→ 表现性测试，必须开放题
- 文学文本可参照朗格"不间断评估"九条按需选取（防止只测初始印象层）

**每一步的检测都要对应第 3 步的教学点。测了没教的东西，是无效检测。**

**检查点**：

- [ ] 每个检测任务都能指回一个教学点
- [ ] 题型与文本体裁匹配（没用客观题测散文的"怎么说"）
- [ ] 量规每一档都含**可检验的证据要求**，不是"理解深刻"这类空话
- [ ] 对标水平号明确可示，且定档合理（必修默认水平 2）
- [ ] 设计了"**如何让个体作答成为全班的讨论对象**"（王荣生的课堂评价第 3 条）
- [ ] 预判了未达标集中的位置及相应的教学调整

**回填母本（本步的收尾动作）**：检测任务与评分量规**回填进母本第十一节**，不另起文件。回填完成、上述检查点全过，**母本即冻结**。此后第 7 步的四份 Word 与课件全部从冻结母本派生；**母本一改，四份 Word 与课件全部作废重出**。

**呈现方式**：检测任务与量规在母本中**单独成节**，不混进教学过程里。用户要单出检测题时，从这一步单独抽取即可；若需独立成卷，见第 7.1 步的可选第五、六份。

### 第 7 步｜四份 Word 交付 + 配套课件 PPT

> **本步按 `交付模式` 分流。**
> `普通备课`：出**四份 Word ＋ 课件 PPT**（7.1 ＋ 7.2），以下原样。
> `精品课`：出**四件套 ＋ 微课运行稿**（下表）。四份 Word 里只有**教学设计**是精品课的提交件；教案／学案／说课稿**不提交**（仍可生成给教师自己用）。

**五件套映射（`精品课`）**：

| # | 提交件 | 本技能怎么出 | 细则 |
|:-:|---|---|---|
| ① | **教学设计** | 母本十二节 ＋ 两附，直接从冻结母本出 | `references/deliverables-word.md` |
| ② | **学习任务单** | **新增独立 `.docx`**——八节结构，**不是把学案改名** | `jpk-high-school-chinese/task-sheet.md` |
| ③ | **课件** | **同一份两阶段 PPT**，只加一层一致性门禁，**不另建课件规范** | `chinese-lesson-design` 的「配套 PPT 交付标准」 |
| ④ | **作业练习** | **新增独立 `.docx`**——检查优先级改为"目标回指 → 课堂承接 → 学习证据 → 评价 → 才轮到分层" | `jpk-high-school-chinese/homework.md` |
| ⑤ | **微课视频** | **只出运行稿**（微课环节时间轴、画面、教师语言、学生任务） | `jpk-high-school-chinese/micro-lesson-runtime.md` |

**两条最容易搞错的地方**：

- **`学案` ≠ `学习任务单`**。两者从**同一母本派生，是两个视图，不是父子关系**；覆盖面、体例、粒度、有无 `目标编号` 都不同。**直接把学案改名交上去是不合格的**——差异表见 `task-sheet.md` §1。
- **微课运行稿不是"把课堂环节除以 3"**。教学设计仍按**完整一节课**做（含七列时间项，"突出课堂实效"要它撑）；微课**另建微课环节时间轴**按自己的节奏重排。硬约束：`微课环节学生任务 ⊆ 课堂学生活动`，视频里冒出课堂设计中不存在的新任务 → **阻断**。

**顺序铁律对精品课同样成立**：**`内容冻结` 之前不派生任何一件；母本一改，五件全部作废重出**，不做局部修补。

**精品课不出的东西**：**教案／学案／说课稿不作为提交件**；检测量规**合入教学设计**，不单独成件。

#### 7.1 四份 Word 交付物（默认交付，不是可选项）

**准入条件（不满足就不做）**：母本已冻结（第 6 步检查点全过）。母本没齐就转 Word，等于把返工写进流程。

从冻结母本派生四份**独立**的 `.docx`，每份各有读者、各有取舍：

| 文件 | 读者 | 装什么 | 绝对不装什么 |
|---|---|---|---|
| `篇目_教学设计.docx` | 教师（自用 / 教研 / 备案） | 母本**全部**十二节 + 两附 | — |
| `篇目_教案.docx` | 教师（上课执行） | 课时安排表（逐环节：**时间项与时长** / 教师活动 / **学生的活动** / 设计意图）、主问题与追问链（含方向备注）、板书、风险点与应对、作业布置、教具与资源 | 文本解读的推导过程、量规三档描写、教学方向 × 学情与教师适配的论证 |
| `篇目_学案.docx` | **学生** | 学习目标（学生视角）、课前预学、课中任务单（读什么—想什么—写什么）、当堂检测、分层作业、学后反思栏 | **教学目标、设计意图、预设与应对、答案、解读结论、教师活动、方向备注、教师适配调整** |
| `篇目_说课稿.docx` | 同行 / 评委 | 说教材、说学情、说目标与重难点、说教法学法、说教学过程（环节 + 意图）、说板书、说作业、说检测与评价、说设计反思 | 教师活动流水账、无意图的环节罗列、不可证的空话 |

**写坏的判据（两处最要紧）**：
- **学案**：每处提问之后若没有书写空间（≥ 2 行），学案就退化成提纲，学生当场用不了。
- **教案**："学生的活动"栏若写成"听讲""思考""配合老师"，说明该环节还没设计完，退回第 3 步。

**生成方式**：每份**分别**加载 `tencent-docx`，把该份对应的内容作为创作材料，走其 `全流程`（第一阶段创作 → 第二阶段美化 → 第三阶段转 `.docx`）产出，产出后按该技能硬约束 `present_files` 打开预览。

- **禁止用 `python-docx` 等裸库直接拼 .docx**——既违反该技能的强制规则，也拿不到它的版式能力。
- **禁止把四份合并成一份多章节文档**——学案里绝不能出现教师版内容。

**顺序铁律**：**母本没冻结，不做 Word；四份一律从母本派生，不许各自另写。** 顺序按教学设计 → 教案 → 学案 → 说课稿，母本先落地，后三份便于随时比对。

**落盘**：与课件同目录（如 `<workspace>/output/<篇目>/`）。文件名形如 `劝学_教学设计.docx`——**不带书名号、不带空格、不带版本号**，版本信息写页眉。

**一致性**：四份的**目标文字逐字一致、课时时间数据一致、主问题文字一致、板书层级一致、作业三档分类一致**。不一致**以母本为准**，改四份。

**可选第五、六份（默认不出）**：用户要独立的课后检测卷 / 习题册时，加出 `篇目_检测卷（学生版）.docx` 与 `篇目_检测卷（教师版·含答案与量规）.docx`。

> **分节细则、Word 版式规范、四份互查清单、常见坑**：见 `references/deliverables-word.md`。本节只记"每份装什么、怎么准入、怎么排顺序"，细则单点维护。

#### 7.2 配套课件 PPT（默认交付，不是可选项）

按 `chinese-lesson-design` 的「**配套 PPT 交付标准**」一节生成课件。**该标准不在本技能复述**——执行时直接加载那个技能的对应章节，七条硬标准、页数分配表、单页规则、配图规范（影像蒸馏）、动画与标题样式、两阶段流程都在那里，单点维护，避免改一处漏一处。

此处只记三条**最容易违反**的硬约束：

- **默认课件版**（上课投给学生）：**不出现教学目标、设计意图、预设应对、教师自述、作业答案、方向备注、教师适配调整**
- **两阶段，不许跳过确认**：先交"页—内容"清单（一页一行，含该页标题与要点）→ 用户确认 → 再调 `tencent-pptx` 生成 `.pptx`
- **页数 ≤ 20 页（含标题页、作者介绍页与小结页）**；正文 ≥ 18pt（目标 22pt）；**每页正文 ≤ 30 字**

**顺序铁律**：**母本没冻结，不生成 PPT。** 母本改一次、PPT 跟着重做一次；反着做会返工两遍。

**一致性**：PPT 与母本的**板书结构、任务顺序、主问题文字**三项必须对应，不一致**以母本为准**；完整检查清单见该技能「与教案的一致性」一节。

**7.1 与 7.2 的先后**：Word 与 PPT 都从母本派生，两者之间没有依赖，但**建议先出四份 Word 再出 PPT**——Word 是文字定稿，PPT 是它的可视化呈现，文字先稳定可以少返工一轮。

### 第 8 步｜材料一致性 ＋ 精品课终检门禁（**仅 `精品课`**）

> 普通模式**没有这一步**；普通模式在 7.1／7.2 的一致性自检后即完成。

**为什么精品课要单设一步**：普通模式只有"母本 → 四份 Word"，四份都是同一母本的视图，靠"以母本为准"一句就能兜住。精品课的五件材料在派生过程中**各自被改写**（任务单要写成学生语言、课件要压到 30 字、微课要重排节奏），由此产生**彼此之间的新偏差**。

**这一步做两件事**：

**（1）五件套一致性检查**——逐字段比对（课题名称／教材版本／目标文字／教学点／任务编号与内容／主问题文字／板书层级／文本引用／作业题目／评价量规／迁移出口／编号体系），**不做"感觉一致"**。三类断链（**横向**＝同一字段两件材料指的不是同一件事、**纵向**＝材料里凭空多出母本没有的元素、**数字**＝课题名／编号／课时／页次对不上）→ 均为 **阻断**。

**修正方向铁律**：不一致时**一律以母本为准，改材料**。
**唯一例外**：若发现**母本本身**有断链（依据链断、目标回指不上教学点），**不许"以母本为准"糊过去**——那会把错误固化。须**退回第 6 步重走 `内容冻结`，五件全部重出**。这正是"冻结"两个字的意义。

**（2）跑门禁一～十，共十道门禁**，出**机器可读**交付报告（YAML，字段与门禁一一对应）。

`终检结论` 算法：**任一阻断 → 阻断**；无阻断但有提醒 → **提醒**；全通过 → 通过。`终检结论 = 通过` 是 `报送冻结` 的前置条件（`提醒` 允许冻，但须把提醒 **逐条列给用户并确认**）。

**两条纪律**：**阻断不许降级为提醒**（`提醒` 是"够用但不完美"，`阻断` 是"错了"；把阻断写成提醒等于把错误藏起来）；**所有提醒必须进交付报告并告知用户**。

**明确不做**：**不输出分数预测**（"预计 92 分""一等奖概率 80%"）。评审不是稳定算法，各地还有地方细化指标。本技能只回答三个问题：**哪里缺证据？哪里断链？哪里有形式主义？**

> 一致性矩阵与九项必查 → `jpk-high-school-chinese/material-consistency.md`；十道门禁判据、分档语义、报告字段 → `quality-gates.md`；提交前终检清单 → `submission-checklist.md`。


---

## 三、五个技能的三处已知冲突与处理

不要掩盖，遇到就向用户交代。

| 冲突                                                                     | 表现                               | 处理                                                       |
| ---------------------------------------------------------------------- | -------------------------------- | -------------------------------------------------------- |
| **任务群与单篇**                                                          | 课标按 18 个任务群组织；王荣生对单元/群文/项目化审慎    | 单元整体设计时两条路都给出，说清各自代价，由用户定                                |
| **"教得深" 与 "教得浅"**                                                     | 孙绍振要挖到深层矛盾；肖培东要"浅浅地教"            | 不矛盾：**深是备课的深度，浅是上课的姿态**。孙绍振的结论不必全进课堂，进课堂的是那个"学生自己走不通的问题" |
| **`chinese-lesson-design` 与 `wang-rongsheng-reading-design` 重叠** | 前者"教学目标/重难点/课时安排"与后者"教学点/台阶"功能重叠 | 见下节分工                                                    |

**重叠技能的分工（本总控裁决）**：

- `wang-rongsheng-reading-design`：**内容决策层**——教什么、为什么教这个、按什么顺序教
- `chinese-lesson-design`：**格式与配套层**——教案九项格式、板书、情境任务链、分层作业、课时表、课件 PPT 交付标准（**学案与说课稿的分节规范尚未在其中定义**，暂由本技能的 `references/xuean-shuoke-spec.md` 承担，见开头的"一处待办"）
- 走全流程时，先王荣生后格式；两者目标不一致时，**以王荣生为准**（有学理依据优于格式完整）

---

## 四、红线（继承各技能的共同底线）

五个技能的共识，也是本流程的底线：

1. **落在具体语言上**——不给出"从表面到表面的滑行"
2. **学生的活动，不是教师的讲解**——教案里凡写"教师讲……"，追问"这段时间学生做什么"
3. **不做文本外的思想教育**——王荣生书中最明确的禁令
4. **不给无法检测的目标**——禁止"培养学生的……情怀"
5. **一节课一个主问题**——其余一律是追问
6. **朗读、讨论、多媒体都不是必需品**——说不出它解决了哪个教学点，就不要。**课件配图同理**：说不出它对应课文哪一句，就是装饰，不放
7. **不编造原文和课时数**——第 0 步的课情卡里，**每个字段都必须标 `已确认` / `推断` / `未提供` / `待核验` 四态之一**。`推断` 要写明推定依据，`未提供` 要问用户或列进"缺失项"，`待核验` 要在母本与交付物里顶着"待核对"字样、不许撤。**填了不标状态，就等于编造**
8. **不用客观题测文学文本的"怎么说"**——体裁与题型错配是本领域最常见的错误
9. **检测必须对应教学点**——测了没教的东西，是无效检测
10. **量规每一档都要有证据要求**——"理解深刻""语言优美"这类表述不可用
11. **课件不出现教学目标与设计意图**——课件是给学生的，不是给听课教师的
12. **课件 20 页封顶（含标题页、作者介绍页与小结页），正文不小于 18pt**——字多就精简或分页，不许缩字号

以下四条为**本流程新增**（第 1–12 条继承自五个底层技能，第 13–16 条由四份 Word 交付引出）：

13. **四份 Word 同源于母本**——母本冻结前不生成；母本一改，四份 Word 与课件**全部作废重出**，不做局部修补
14. **学案是给学生的**——不得出现教学目标、设计意图、预设与应对、**答案**、解读结论，以及**方向备注与教师适配调整**（后两项是给教师看的）。学案一出现答案，课堂讨论立即失效
15. **四份文件分四份出**——不合并成一份多章节文档；生成走 `tencent-docx`，不裸调 `python-docx`
16. **Word 里的板书用层级表格承载**——不贴 ASCII 字符画（等宽字体在 Word 中会错位）；课件里的板书仍用原生图形重画

以下十条为**精品课模式（`精品课`）新增**，接上面的 1–16 条（**红线 1–16 在本模式下同样有效，一条不放松**）。展开理由见 `references/jpk-high-school-chinese/quality-gates.md`：

17. **官方节点名称不一致，不得提交**——标题须与平台节点**字符串全等**，不得用相似度判定
18. **没有 `目标 → 活动 → 证据 → 评价` 闭环，不得冻结**——教学评一致是本模式最高优先级门禁
19. **学习任务单不得脱离目标单独生成**——每个任务都要挂 `目标编号`
20. **作业必须能回指至少一个目标**——回指不上就不留
21. **视频核心任务必须与教学设计一致**——`微课环节学生任务 ⊆ 课堂学生活动`
22. **课件中的核心任务不得脱离母本**——课件是母本的视图，不是第二个事实源
23. **使用技术必须说明解决的教学问题**——说不出就删；**不得出现"必须使用 AI／技术亮点"这类表述**
24. **技术操作不得成为主要学习负担**——学生操作工具的时间不得多于语文任务时间
25. **五件套中的课题、教材、目标、任务不得互相冲突**——一致性检查逐字段给结论，不做"感觉一致"
26. **提交前必须过精品课终检门禁**——`终检结论 = 通过` 或 `提醒`（提醒须逐条告知用户）方可 `报送冻结`
27. **交付物一律用中文**——五件套与四份文档的正文**不得出现英文与英文缩写**；编号用中文（`目标一`／`教学点一`／`门禁四`），结论用中文（`通过`／`提醒`／`阻断`／`未提供`）。**内部字段名与过程标识只用于流程记录，不得抄进交付物**；确实无法中文化的技术专名（文件名、格式名、工具名）与拼音注音除外

> **一句话记住 17–27**：**节点不能错、链条不能断、材料不能互相打架、技术不能反客为主、门禁不能不过、交付物不能见英文。**

---

## 五、给用户的呈现方式

一次完整交付，按此顺序呈现：

**开场先给课情卡（第 0 步的产物）**：一张七组字段的表格，**每个字段都带 `已确认` / `推断` / `未提供` / `待核验` 四态标记**。其后单列三个栏目并请用户确认——「**请核**」（`推断`，附推定依据）、「**缺失项**」（`未提供`，附所用假设）、「**待核对**」（`待核验`，请用户核实）。**这三个栏目不许省，省了红线 7 就落空了**；**必问四项未落定前不进入第 1 步**（可缺省项允许带标记先行推进，但须在栏目里挂出来）。

然后是：

1. **一句话定位**：这篇课文在单元中的位置 + 本课要解决的核心问题
2. **依据**（简）：任务群 + 水平 + 素养落点，各一行
3. **文本解读要点**：只给会进课堂的那几条，不给全部读解成果
4. **教学点与台阶**（表格；"用时"按**七列时间项**拆开，汇报写出组数）
5. **课堂设计**：出口 + 主问题（附方向备注）+ 朗读 + 风险点（三段式）
6. **教学设计母本**（十二节 + 两附）：会话内先以 md 呈现供确认，不直接甩出四份文档
7. **检测任务与量规**：回填进母本第十一节，含对标水平号与分档表现描述
8. **四份 Word 交付**：母本冻结后一次性生成，给出**四份文件路径 + 一致性自检结论**（哪一项对齐了、哪一项以母本为准改过）
9. **配套课件 PPT**：**先交"页—内容"清单交用户确认，确认后才生成 `.pptx`**；课件与母本同源，标准见 `chinese-lesson-design` 的「配套 PPT 交付标准」
10. **教学方向 × 学情 + 教师适配**：三个方向的差异各一句（主问题旁已附方向备注）；**主打方向那一行**的三格差异点单列一段；教师画像为 `未提供` 时注明"**本次未做教师适配**"

**用户只要检测时**：跳过 1–5、8–9，直接给第 7 项，但**必须先问清教学点**——不知道教什么，检测无法设计。

**用户只要 Word 时**：先确认母本在哪——**母本没冻结就不做 Word**（四份文档的内容全部来自母本，凭空拼出来的文档必然和课堂脱节）。有冻结母本则直接进入第 7.1 步，四份按序分四份出。

**用户只要说课稿 / 学案时**：同理，先要母本。**没有母本就写不出说课稿**——说课稿的每一节都要回答"为什么这样设计"，而那个"为什么"写在母本的依据与预设里。

**用户只要课件时**：先确认母本在哪——**没有冻结母本就不做课件**（课件的内容全部来自母本，凭空做出来的课件必然和课堂脱节）。有母本则直接进入 7.2，第一步先交页面清单。

**呈现要求**：

- 先给判断，再给理由。不写"这是一个很好的问题"这类铺垫
- 每个设计选择都要能说清"为什么这样而不是那样"
- **教学方向与学情适配必须留**——不替用户定唯一的"最佳教案"，三个方向各给一句差异，由用户挑定；不默认一种学情
- 时间表要给**分项**，不只给总数——总数对得上但漏了汇报组数或切换时间，课堂照样超时
- 有分歧（如任务群与单篇）就明说，附依据，不替用户决定

### 精品课模式（`精品课`）的呈现顺序

**开场与普通模式不同：先给节点，不先给设计。** 节点没锁，后面都是白做。

**第一阶段（第 0.5 步完成时）**——先给三样：

1. **`提交` 第八组字段表**（含四态标记）
2. **节点卡与核验结论**：`官方节点名` 原文 ＋ **五项布尔逐项列出**（不许合成一个"通过"）＋ `来源` 三件（URL／核对时间／证据）
3. **一句话结论**：`通过` 继续；`阻断` **停下说明原因**；证据拿不到 → **明说无法推进，不推定通过**

**第二阶段（节点通过后）**——按顺序给（与普通模式第 1–8 项相同处不重复展开）：

4. **依据链七跳**（节点→教材→单元→任务群→水平→素养→目标），每跳一行；**教学点与台阶、课堂设计**同普通模式第 4、5 项
5. **母本**（十二节 ＋ 两附）＋ **教学评一致性矩阵**（十列）与检测量规
6. **`内容冻结` 声明**——明说"以下五件均从这份冻结母本派生，母本一改五件全部作废重出"
7. **四件套 ＋ 微课运行稿**：每件给文件路径；任务单与作业单列；微课给微课环节时间轴；**并明确说"微课只出运行稿，不生成 MP4"**
8. **一致性检查结论**（逐项 `通过／提醒／阻断`，每条写清"哪一件、哪一处、差在哪"）＋ **精品课交付报告**与 `终检结论` 值（`提醒` 逐条说明）＋ **`报送冻结` 声明** ＋ **已知留白告知**（视频技术参数与素材版权清单**不在这份工作范围内**，请走录制与提交环节）

**呈现要求（精品课另加三条）**：**节点结论单独成段**，不要埋在设计说明里（用户的第一决策点是"这个节点能不能交"）；**一致性检查逐项给结论**，不做"感觉一致"；**交付报告必须说明不做的事**（不预测分数、不出视频参数、不做版权清单），避免用户误以为已覆盖。

---

## 六、参考

- 各技能的详细内容，直接加载对应技能，本技能不复述
- 交付格式模板见 `chinese-lesson-design` 与 `wang-rongsheng-reading-design` 的"交付格式"段（注意：`chinese-lesson-design` 目前**只有教案九项**，未定义学案与说课稿）
- **四份 Word 交付物**：`references/deliverables-word.md`——四份文件的分节规范（装什么 / 绝对不装什么）、Word 版式与落盘规范、生成流程（`tencent-docx` 的 `全流程`）、四份互查清单、常见坑、写作禁忌。**本技能不复述该细则，执行第 7.1 步时直接加载。**
- **第 0 步的课情卡**：`references/lesson-context.md`——课情卡的完整字段表（七组）、`已确认`／`推断`／`未提供`／`待核验` 四态的判定决策树、对用户呈现的表格模板、三个栏目（「请核」／「缺失项」／「待核对」）的交付话术、与下游四个步骤的对接点、六种常见违规、第 0 步检查清单。**第 0 步开始时先读它；红线 7 的落地形态就在这里。**
- **时间预算**：`references/time-budget.md`——**七列时间项**（六类必计项 ＋ 教师讲授·齐读·机动：独立阅读 / 小组讨论 / 汇报×组数 / 教师回应 / 书写 / 切换组织 / 教师讲授·齐读·机动）与各自的计提规则、"汇报 × 组数"的算法、母本第五节表的填法与填好的示例、两条硬约束（Σ 误差 0；**不许压缩学生时间凑数**）、认知负荷（可选）、六种常见违规、检查清单。**填母本第五节前先读它**，别拍数字。
- **教学方向 / 学情 / 教师画像**：`references/differentiation.md`——三个方向（基础型 / 思辨型 / 高考型）的**一句话差异**（供"提问旁备注"用）、3×3「方向 × 学情」矩阵与收缩规则、教师画像字段与应用规则（含"**不进学案与课件**"的边界）。**写母本第七节与第十二节前先读它**。
- **学案与说课稿的体例依据**：`references/xuean-shuoke-spec.md`——两份文件的分节表、语文分体裁栏目差异、量化约束（学案 ≤ 30 分钟 / 说课稿字数按时长倒推）、红线与检查清单，**每条附校本或教研来源与可信度分级**。写这两份前先读它，不要凭印象编栏目。
- **文档生成通道**：Word 走 `tencent-docx`，课件走 `tencent-pptx`。两者都是外部技能，本技能只规定"每份装什么、不装什么"，版式与工具选择由它们决定
- **检测与评价**：`wang-rongsheng-reading-design/references/detection-design.md`（测什么、怎么选题型）+ `gaozhong-yuwen-kebiao/references/ceping-renwu-zhuanhua.md`（水平转量表、量规模板）
- **课件 PPT**：`chinese-lesson-design` 的「配套 PPT 交付标准」——七条硬标准、页数分配表、单页规则、**配图规范（影像蒸馏：以课文景物描写为源，蒸馏生成，插在留白充足的页面；作者介绍页配写意形象）**、动画与标题样式、两阶段流程、生成方式（`tencent-pptx` 生成，生成后改内容走 `tencent-local-office-edit`）。**本技能不复述该标准，执行时直接加载那一节。**
- 流程速查表见 `references/flow-checklist.md`
- **学科课程类精品课（`精品课`）**：`references/jpk-high-school-chinese/`——**11 个文件，只服务精品课这一个模式**（导航 `README.md`／年度政策 `2026-rules.md`／节点锁 `catalog-node.md`／一致性 `alignment.md`／技术适切 `technology-fit.md`／微课运行稿 `micro-lesson-runtime.md`／任务单 `task-sheet.md`／作业 `homework.md`／材料一致性 `material-consistency.md`／门禁 `quality-gates.md`／终检 `submission-checklist.md`）。
  **加载顺序见该子目录 `README.md`，执行前先读它**；本技能**不复述这 11 个文件的细则**，按步骤单点加载。
- **版本与变更记录**：`CHANGELOG.md`——按语义化版本记录每次改动。**当前版本就是本文件 frontmatter 的 `version:` 字段**，每个版本对应仓库中的一个 git 标签（`v1.0.0` …）。引用本技能时请带上版本号，不同版本的流程与红线数量不同。
