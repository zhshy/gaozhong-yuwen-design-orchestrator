# 01 节点卡与核验结论（第 0.5 步）

> ⚠️ **本文件是演示样本，不是真实核验记录。**
>
> 平台节点原文与截图**未实际取得**；下表中 `official_title` 取**教材目录**的写法，五项布尔为**演示值**。
> 真实使用时的纪律是：**没有证据就写 `unknown`，五项中任一 `unknown` 即 BLOCK，流程停在 0.5 步，不许推定通过。**
> 本例把它填成 `PASS`，**仅为了让后面的九步能演示完**——这不是可以照抄的做法。
> 正式提交前，必须由用户从平台目录页**原文复制**节点名并提供截图。

---

## 一、`CatalogNode`

| 组 | 字段 | 值 | 状态 |
|---|---|---|---|
| 平台 | `platform.name` | 国家中小学智慧教育平台 | `confirmed` |
| 平台 | `platform.year` | 2026 | `confirmed` |
| 学科定位 | `subject` | 语文 | `confirmed` |
| 学科定位 | `stage` | 高中 | `confirmed` |
| 学科定位 | `grade` | 高一 | `confirmed` |
| **标题** | **`official_title`** | `劝学` | **`needs_verification`**（演示值；须平台原文） |
| **标题** | **`submitted_title`** | `劝学` | `confirmed` |
| 教材 | `textbook.publisher` | 人民教育出版社 | `confirmed` |
| 教材 | `textbook.edition` | 2019 年版 | **`needs_verification`**（须与用书版权页核对） |
| 教材 | `textbook.volume` | 必修上册 | `confirmed` |
| 节点标识 | `node_id` | （演示留空） | `missing` |
| 取证 | `source.url` | （演示留空——**真实使用时必填**） | `missing` |
| 取证 | `source.checked_at` | （演示留空——**真实使用时必填**） | `missing` |
| 取证 | `source.evidence` | （演示留空——**截图必附**） | `missing` |

**⚠️ 特别注意上表三处 `missing`**：真实核验里，`source` 三件（URL / 核对时间 / 截图）**缺一即视为无证据**，而**无证据即 BLOCK**。本例留空是为了标明它们不可省。

---

## 二、五道核验（逐项，不许合成一个"通过"）

| # | 判据 | 布尔 | 说明 |
|:-:|---|:-:|---|
| ① | `node_exists` 节点存在 | `true` | **演示值** |
| ② | `subject_match` 学段＝高中、学科＝语文 | `true` | **演示值** |
| ③ | `grade_match` 年级对应 | `true` | **演示值** |
| ④ | `textbook_match` 教材版本对应 | `true` | **演示值**；版次一项仍待与学生用书核对 |
| ⑤ | `title_exact_match` 标题**全等** | `true` | **演示值**；须以平台原文逐字比对 |

**`status`＝`PASS`（演示值）。**

**真实的判定规则**（照此执行，不要照抄上表）：

```
任一项为 unknown  → BLOCK
title 不完全一致   → BLOCK
学科不是高中语文   → BLOCK
教材版本无法确认   → BLOCK（可先 needs_verification，补齐证据后复核）
```

---

## 三、为什么标题必须"字符串全等"

**这是本例最想演示的一条纪律。**

在《劝学》这个篇目上，平台节点至少可能是以下写法之一：

```
劝学
劝学（节选）
荀子《劝学》
第六单元 劝学
```

**它们看起来"都是《劝学》"，但它们是不同的节点。**

| 用法 | 允许？ |
|---|:-:|
| 模糊匹配用于**检索**（"我记得是《劝学》"→ 搜出候选） | ✅ |
| 模糊匹配用于**判定**（"95% 像，就是它"） | ❌ **禁止** |

**相似度高，恰恰说明它可能是另一个节点。** 用相似度判定，等于把"没核对"包装成"核对过了"。

**发现名字不一致时**：**不要改节点名去迁就设计**，也不要改设计去迁就一个含糊的记忆——把两个名字**并列摆给用户**，请他**从平台页面直接复制**节点原文。

---

## 四、降级边界（本模式唯一不让步的地方）

本技能**没有**智慧平台的查询接口，节点核验只有两条路：

1. **用户提供**平台截图 / 页面原文；
2. 本技能**联网检索**后，标注来源 URL 与核验时间。

> **两条路都拿不到证据时，不得降级为"推定通过"。**
> 按"核心正确性无法保障"处理：**停止流程，说明原因**，请用户补证据。

**为什么这一处不让步**：其余各步都能在流程内部被修正（记 `WARN`、记 `needs_verification` 先往下走）；而**节点是流程的输入**——输入错了，后面每一步都会忠实地把错误放大。

**照此交付的话术**：

> "节点这一项我查不到可靠依据，**不能替你推定**。请你从国家中小学智慧教育平台的目录页把节点名称**原文复制**给我（截图也行），我核对完再往下做。这一步做错，后面四件材料都要重做。"

---

## 五、检查清单

- [ ] `official_title` 是**从平台原文复制**的（本例为**演示值，未达标**）
- [x] `official_title === submitted_title` 已逐字比对（本例两者同为 `劝学`）
- [x] 五项布尔**逐项**列出，没有合成一个"通过"
- [x] `textbook` 记到**出版社／版次／册次**（版次待与学生用书核对）
- [ ] `source` 三件齐全（URL ＋ 核对时间 ＋ 截图）——**本例未达标，已在 §一 标出**
- [x] `status` 有明确结论
- [x] 若为 `BLOCK`：已停止并说明原因
- [x] 若证据拿不到：已明确告知无法推进，**没有推定通过**

> **本清单有两项打不上勾**——这正是本示例要示范的：**技能会把"没做到"如实标出来，而不是把演示值当结论。**
